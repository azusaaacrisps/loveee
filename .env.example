# === 环境变量模板（可提交，不含任何密钥） ===
#
# 网易云音乐 API 地址（前端只需要地址，绝不要把 AppID/AppSecret/私钥写到这里！
# 那些凭证只配置在你的私人 API 服务端）。
#
# 开发环境（.env）：填 /netease，由 Vite 代理到本地 3001 的私人 API 服务
# 生产环境（.env.production 或 GitHub 仓库变量）：填私人 API 部署后的公网地址，
# 例如 https://your-netease-api.example.com
VITE_NETEASE_API_BASE=/netease
