# === 生产环境（GitHub Pages）专用，本文件已被 .gitignore 忽略 ===
#
# GitHub Pages 是纯静态托管，Vite 的 /netease 代理不存在，
# 所以这里必须填你的私人 API 部署到公网后的完整地址，例如：
# VITE_NETEASE_API_BASE=https://your-netease-api.example.com
#
# 留空则前端在生产环境回退到 /netease（会 404），部署前务必填写。
VITE_NETEASE_API_BASE=https://loveee-ncm-api.onrender.com
